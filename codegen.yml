overwrite: true
schema: http://localhost:4001/graphql
documents: '../gqlserver/src/schemas/**'
generates:
  ./gql/sdk.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
      - plugin-typescript-swr  
config:
  scalars:
    DateTime: string
    Date: string
         
          